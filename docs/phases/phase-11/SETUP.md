# Phase 11 setup

Phase 11 introduces no credentials, environment variables, services, or database migrations.

Follow the repository-root `SETUP.md` to run PostgreSQL, the AI service, Spring backend, and Next.js frontend. Existing AI-provider credentials are still required for a live interview and generation smoke test.
